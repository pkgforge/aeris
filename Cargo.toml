[package]
name = "aeris"
version = "0.0.0"
edition = "2024"
description = "Unbounded package management"
license = "MIT"

[features]
debug = ["iced/hot"]

[dependencies]
iced = { version = "0.14.0", default-features = false, features = [
  "thread-pool",
  "tiny-skia",
  "wayland",
  "x11"
] }

[profile.release]
opt-level = "z"
strip = true
lto = true
panic = "abort"
codegen-units = 1
